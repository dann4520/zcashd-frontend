package zcash.frontend

class DashboardController {

    def showPeerInfo() {
        // make jsonrpc call

        // parse results

        // return results as json to be picked up by view
        render "showPeerInfo()"
    }
}
